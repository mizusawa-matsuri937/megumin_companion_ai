# 只填写专用于本项目、额度受限的密钥；不要填写或提交真实生产密钥。
# Day 3 人工脱敏验收时请使用明确无效的假值，例如 fake-day3-review-key。
COMPANION_LLM_API_KEY=replace_me

# 可选配置覆盖；非敏感设置优先写入 config.yaml。
# MEGUMIN_LOG_LEVEL=DEBUG
# MEGUMIN_SERVER_PORT=8765
