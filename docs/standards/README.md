# 规范与约束

这里存放可长期复用的工作规范、证据标准和审计约束。不可协商的短版规则仍在根目录
[`AGENTS.md`](../../AGENTS.md)，本目录提供展开说明，不能放宽 `AGENTS.md`。

- [`AGENT_OPERATING_CONSTRAINTS.md`](AGENT_OPERATING_CONSTRAINTS.md)：证据等级、文档维护、隐私边界、
  AI 优先审计和人工 Gate 的执行口径。

新的跨任务规范必须写明适用范围、强制级别、证据要求和与现有规则的关系；不要把一次性 Wxx 实现细节放在这里。
